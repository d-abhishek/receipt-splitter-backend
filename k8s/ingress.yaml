apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: receipt-splitter-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: receipt-splitter-service
                port:
                  number: 80